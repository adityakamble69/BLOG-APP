/* ==========================================================================
   MARGINALIA — config
   Base URL of your Node.js/Express backend (see /backend). Change this if
   your server runs on a different host/port, or when you deploy.
   ========================================================================== */
const CONFIG = {
  API_BASE: "http://localhost:5000/api"
};
